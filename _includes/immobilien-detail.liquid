---
layout: base.njk
---

<div class="container mx-auto px-4 py-16 bg-page-bg">

    <a href="/angebote.html" class="inline-block mb-8 text-primary hover:text-secondary font-semibold">
        <i class="fas fa-arrow-left mr-2"></i> Zurück zu den Angeboten
    </a>

    <div class="bg-white rounded-xl shadow-2xl overflow-hidden p-6 md:p-10">

        <header class="mb-8 border-b pb-4 border-gray-100">
            <span class="inline-block bg-secondary text-primary text-sm font-semibold px-4 py-1 rounded-full mb-3">{{ status }}</span>
            <h1 class="text-4xl font-extrabold text-primary mb-2">{{ titel }}</h1>
            <p class="text-xl text-gray-600">{{ ort }}</p>
            <p class="text-4xl font-black text-accent mt-4">€ {{ preis }}</p>
        </header>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">

            <div class="lg:col-span-2">
                <img src="{{ hauptbild }}" alt="Hauptbild von {{ titel }}" class="w-full h-auto object-cover rounded-lg shadow-lg mb-8 border border-gray-200">

                <h2 class="text-2xl font-bold text-primary mb-4 border-b border-secondary/50 pb-2">Objektbeschreibung</h2>

                <div class="prose max-w-none text-gray-700">
                    {{ content | raw }}
                </div>
            </div>

            <div class="lg:col-span-1 space-y-8">

                <div class="bg-page-bg p-6 rounded-lg shadow-inner">
                    <h3 class="text-xl font-bold text-primary mb-4 border-b border-accent/50 pb-2">Wichtige Eckdaten</h3>
                    <ul class="space-y-3 text-gray-700">
                        <li class="flex justify-between border-b pb-1"><span class="font-semibold text-primary">Ort:</span> <span>{{ ort }}</span></li>
                        <li class="flex justify-between border-b pb-1"><span class="font-semibold text-primary">Wohnfläche:</span> <span>{{ eckdaten.flaeche }} m²</span></li>
                        <li class="flex justify-between border-b pb-1"><span class="font-semibold text-primary">Anzahl Zimmer:</span> <span>{{ eckdaten.zimmer }}</span></li>
                        <li class="flex justify-between border-b pb-1"><span class="font-semibold text-primary">Baujahr:</span> <span>{{ eckdaten.baujahr }}</span></li>
                        <li class="flex justify-between pt-2"><span class="font-bold text-primary">Kaufpreis:</span> <span class="text-accent font-bold">€ {{ preis }}</span></li>
                    </ul>
                </div>

                <div class="p-6 bg-primary text-white rounded-lg text-center shadow-lg">
                    <h3 class="text-2xl font-bold text-accent mb-3">Interesse?</h3>
                    <p class="mb-4 text-secondary">Vereinbaren Sie noch heute einen Besichtigungstermin.</p>
                    <a href="/index.html#kontakt" class="w-full inline-block bg-accent text-primary font-bold py-3 rounded-lg hover:bg-white transition duration-300">
                        Kontakt aufnehmen
                    </a>
                </div>

            </div>
        </div>

    </div>
</div>