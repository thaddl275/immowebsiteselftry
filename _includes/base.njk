<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Fischer-Geserich Immobilien</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'primary': '#0B0B45', // Navy Blue (Kontrastfarbe/Seriosität)
                        'secondary': '#89CFF0', // Baby Blue (Akzentfarbe/Text)
                        'accent': '#FFD700', // Gold (Buttons, Sterne, Akzente)
                        'page-bg': '#E9EEF0', // Off-White Hintergrund
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-page-bg">

<nav class="bg-primary text-white shadow-lg">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center py-4">
            <div class="text-2xl font-bold">
                <a href="/index.html" class="flex items-center">
                    <i class="fas fa-city mr-2 text-accent"></i>
                    Fischer-Geserich Immobilien
                </a>
            </div>

            <div class="md:hidden">
                <button id="mobile-menu-button" class="text-white focus:outline-none">
                    <i class="fas fa-bars text-2xl"></i>
                </button>
            </div>

            <div class="hidden md:flex space-x-8 items-center">
                {% set currentUrl = page.url | url %}
                {% set activeClass = "text-accent font-bold border-b-2 border-accent pb-1" %}
                {% set defaultClass = "hover:text-accent" %}

                <a href="/index.html"
                   class="{% if currentUrl == '/' or currentUrl == '/index.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Start
                </a>

                <a href="/angebote.html"
                   class="{% if currentUrl == '/angebote.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Angebote
                </a>

                <a href="/ueber-uns.html"
                   class="{% if currentUrl == '/ueber-uns.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Über uns
                </a>

                <a href="/ankaufsberatung.html"
                   class="{% if currentUrl == '/ankaufsberatung.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Ankauf
                </a>

                <a href="/finanzierungsberatung.html"
                   class="{% if currentUrl == '/finanzierungsberatung.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Finanzierung
                </a>

                <a href="/verwaltung.html"
                   class="{% if currentUrl == '/verwaltung.html' %}{{ activeClass }}{% else %}{{ defaultClass }}{% endif %}">
                    Verwaltung
                </a>

                <a href="/index.html#kontakt" class="hover:text-primary bg-accent text-primary px-4 py-2 rounded font-semibold transition duration-300 hover:bg-white">Kontakt</a>
            </div>
        </div>

        <div id="mobile-menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col space-y-2">
                {% set mobileActiveClass = "block px-3 py-2 bg-accent text-primary rounded font-semibold" %}
                {% set mobileDefaultClass = "block px-3 py-2 hover:bg-primary/90 rounded" %}

                <a href="/index.html"
                   class="{% if currentUrl == '/' or currentUrl == '/index.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Start</a>

                <a href="/angebote.html"
                   class="{% if currentUrl == '/angebote.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Angebote</a>

                <a href="/ueber-uns.html"
                   class="{% if currentUrl == '/ueber-uns.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Über uns</a>

                <a href="/ankaufsberatung.html"
                   class="{% if currentUrl == '/ankaufsberatung.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Ankauf</a>

                <a href="/finanzierungsberatung.html"
                   class="{% if currentUrl == '/finanzierungsberatung.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Finanzierung</a>

                <a href="/verwaltung.html"
                   class="{% if currentUrl == '/verwaltung.html' %}{{ mobileActiveClass }}{% else %}{{ mobileDefaultClass }}{% endif %}">Verwaltung</a>

                <a href="/index.html#kontakt" class="{{ mobileDefaultClass }}">Kontakt</a>
            </div>
        </div>
    </div>
</nav>


<main>{{ content | safe }}</main>


<footer class="bg-gray-900 text-white py-8">
    <div class="container mx-auto px-4">
        <div class="flex flex-col md:flex-row justify-between items-center">
            <div class="mb-4 md:mb-0">
                <h3 class="text-xl font-bold flex items-center">
                    <i class="fas fa-city mr-2 text-accent"></i> Fischer-Geserich Immobilien
                </h3>
                <p class="text-gray-400 mt-1">Ihr Partner für Immobilienlösungen in Berlin/Brandenburg</p>
            </div>

            <div class="flex space-x-6">
                <a href="#" class="text-gray-400 hover:text-accent transition duration-300">
                    <i class="fab fa-facebook-f text-xl"></i>
                </a>
                <a href="#" class="text-gray-400 hover:text-accent transition duration-300">
                    <i class="fab fa-linkedin-in text-xl"></i>
                </a>
                <a href="#" class="text-gray-400 hover:text-accent transition duration-300">
                    <i class="fab fa-instagram text-xl"></i>
                </a>
            </div>
        </div>

        <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400 text-sm">
            <p>
                &copy; 2025 Fischer-Geserich Immobilien. Alle Rechte vorbehalten.
                | <a href="impressum.html" class="hover:underline">Impressum</a>
                | <a href="datenschutz.html" class="hover:underline">Datenschutz</a>
            </p>
        </div>
    </div>
</footer>



<script>
    const mobileMenuBtn = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');

    mobileMenuBtn?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
    });
</script>
<script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</body>
</html>